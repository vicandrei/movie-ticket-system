cd path/to/movie_ticket_system

# Initialize Git (if you haven’t already)
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit: Flask ticket app with PDF generation"

# Link to your GitHub repo (replace <your‑username> below)
git remote add origin https://github.com/<your-username>/movie-ticket-system.git

# Push
git branch -M main
git push -u origin main
